version 17.0.0.1 (Dt. 23/01/2024)
     => Fixed:- => Fixed issue whenever select the date then slot table is open according date wise not day wise.
     => date field added in appointement.slots.
     
version 17.0.0.2 (Dt. 25/01/2024)
     => Fixed:- => invisible column of company_id, currency_id, account_id, repair_id fields of timesheet notebook page.
